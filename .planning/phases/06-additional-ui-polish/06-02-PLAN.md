---
phase: 06-additional-ui-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [src/components/layout/Footer.tsx]
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Session status is on its own line above keybinding hints"
    - "No duplicate keybinding hints in footer"
    - "Keybinding hints are cleanly separated and readable"
  artifacts:
    - path: "src/components/layout/Footer.tsx"
      provides: "Footer with restructured layout"
      contains: "marginBottom"
  key_links:
    - from: "src/components/layout/Footer.tsx"
      to: "src/App.tsx"
      via: "activeTab prop"
      pattern: "activeTab.*props"
---

<objective>
Reorganize Footer layout with session status on separate line and deduplicated hints

Purpose: Improve footer readability by separating session status from keybinding hints and removing duplicate hints
Output: Cleaner footer with session status above keybinding hints, no duplication
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/components/layout/Footer.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move session status to its own line</name>
  <files>src/components/layout/Footer.tsx</files>
  <action>
  Restructure Footer component to have two separate lines:

  1. Change main Box (line 63) from single paddingX to flexDirection="column" with marginTop:
  ```tsx
  <Box flexDirection="column" marginTop={1} paddingX={1}>
  ```

  2. Extract session status into its own Box with marginBottom:
  ```tsx
  <Box flexDirection="column" marginTop={1} paddingX={1}>
    {/* Session status line */}
    <Text dimColor>
      {activity?.isActive && activity.currentActivity && (
        <Text color="cyan" bold>
          ● {activity.currentActivity}
        </Text>
      )}
      {activity && !activity.isActive && activity.title && (
        <Text dimColor>○ {activity.title.slice(0, 8)}...</Text>
      )}
    </Text>

    {/* Keybinding hints line */}
    <Text dimColor>
      {allHints.map((hint, index) => (
        <Text key={hint.key}>
          {index > 0 && ' | '}
          <Text bold>{hint.key}</Text>: {hint.action}
        </Text>
      ))}
    </Text>
  </Box>
  ```

  This creates two separate lines: one for session status (if any), one for keybinding hints.
  </action>
  <verify>grep -A5 'flexDirection="column"' src/components/layout/Footer.tsx | grep 'marginBottom' && grep -c 'Text dimColor>' src/components/layout/Footer.tsx | grep -q '^2$'</verify>
  <done>Session status displays on separate line above keybinding hints</done>
</task>

<task type="auto">
  <name>Task 2: Remove duplicate keybinding hints</name>
  <files>src/components/layout/Footer.tsx</files>
  <action>
  Remove duplicate "[/]" hint from phase view hints since it's now in local hints or removed.

  Current phase hints (lines 28-32):
  ```tsx
  phase: [
    { key: '\u2191\u2193', action: 'scroll' },
    { key: '[/]', action: 'switch phase' },
    { key: 'e', action: 'edit' },
  ],
  ```

  Change to:
  ```tsx
  phase: [
    { key: '\u2191\u2193', action: 'scroll' },
    { key: 'e', action: 'edit' },
  ],
  ```

  This removes the "{ key: '[/]', action: 'switch phase' }" line since Plan 01 already removed local navigation hints from PhaseView.tsx (which contained `[: prev | ]: next`). Users will still know about [ and ] for phase switching from Footer's original implementation before this change.
  </action>
  <verify>! grep -q "'\\[/\\]'" src/components/layout/Footer.tsx</verify>
  <done>No duplicate hints in footer - [ and ] not shown in phase hints</done>
</task>

</tasks>

<verification>
- Run `bun run dev` and navigate to phase tab (Tab 2)
- Verify footer has two lines: session status (if active) on top, keybinding hints below
- Press Tab 1 to switch to roadmap - verify footer shows roadmap hints cleanly
- Check that no duplicate keybinding hints appear in footer
</verification>

<success_criteria>
- Session status appears on its own line when active
- Keybinding hints appear on separate line below session status
- No duplicate hints in footer (no "[/]" or similar duplicates)
- Footer remains readable and uncluttered
</success_criteria>

<output>
After completion, create `.planning/phases/06-additional-ui-polish/06-02-SUMMARY.md`
</output>
