---
phase: 06-additional-ui-polish
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/phase/PhaseView.tsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Phase tab content fits without bordered box"
  artifacts:
    - path: "src/components/phase/PhaseView.tsx"
      contains: "borderStyle=\"single\""
      expect: false
  key_links: []
---

<objective>
Remove bordered box from PhaseView component to eliminate overflow issues with long phase content.

Purpose: User reported "phase 5 still exceeds the size of the box... let's just remove that box. we don't need it." Phase 5 has 18 plans generating substantial text that overflows the border. Ink doesn't support true viewport clipping.

Output: PhaseView displays content without bordered box wrapper.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-additional-ui-polish/06-additional-ui-polish-UAT.md
@src/components/phase/PhaseView.tsx

# Gap details:
# Test 2 failed: "When phase tab content exceeds viewport height, it scrolls while the footer remains visible at bottom of screen"
# User reported: "phase 5 still exceeds the size of the box... let's just remove that box. we don't need it."
# Root cause: borderStyle="single" creates box that content overflows in Phase 5. Ink doesn't support true viewport clipping - content always renders fully regardless of box boundaries.
# Fix: Remove borderStyle="single" from Box at line 214 in PhaseView.tsx
</context>

<tasks>

<task type="auto">
  <name>Remove bordered box from PhaseView</name>
  <files>src/components/phase/PhaseView.tsx</files>
  <action>
  In src/components/phase/PhaseView.tsx:

  At line 214, remove borderStyle="single" from the Box component that wraps phase content.

  The line should change from:
  ```tsx
  <Box flexDirection="column" borderStyle="single" paddingX={1} flexGrow={1}>
  ```

  To:
  ```tsx
  <Box flexDirection="column" paddingX={1} flexGrow={1}>
  ```

  Keep all other props (flexDirection, paddingX, flexGrow) unchanged.
  </action>
  <verify>
  grep -q "borderStyle" src/components/phase/PhaseView.tsx && echo "borderStyle still present" || echo "borderStyle removed"
  # Expected: borderStyle removed (grep returns non-zero)
  </verify>
  <done>
  PhaseView displays content without bordered box. Phase 5 with 18 plans no longer has visual overflow issues.
  </done>
</task>

</tasks>

<verification>
Run TUI with `bun run dev` and verify:
- Navigate to Phase 5 (which has 18 plans)
- Phase tab content displays without bordered box
- No visual overflow issues
- Content still readable and well-organized
- Footer remains visible at bottom of screen
</verification>

<success_criteria>
- PhaseView.tsx has no borderStyle prop on phase content Box
- Phase 5 content displays without overflow
- Visual hierarchy still clear without border
</success_criteria>

<output>
After completion, create `.planning/phases/06-additional-ui-polish/06-06-SUMMARY.md`
</output>
